<div id='<%= dom_id cart %>'>
  <!-- page`s title -->
  <div>
    <h1 class='pt-5 mt-5 text-center tracking-tighter antialiased font-semibold text-2xl text-stone-700/75'>Shopping Cart</h1>
    </br>
  </div>

  <!-- cart products list -->
  <div class=''>
    <%= render cart.line_items %>

    <div class='flex inline-block border-t-2 border-stone-400 justify-end'>
      <div class='text-right pt-5 text-stone-500'> Total:</div>
      <div class='text-right pt-5 pl-3 font-bold'> <%= number_to_currency(cart.total_price) %> </div> </td>
    </div>
  </div>
</div>
